package com.telecom.billing.model;

public enum CustomerTier {
    A,   // Premium  — highest discount, fastest SLA
    B,   // Standard — mid discount
    C    // Basic    — no discount, longest SLA
}
